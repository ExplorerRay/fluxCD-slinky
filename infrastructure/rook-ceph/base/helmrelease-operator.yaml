---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: rook-ceph
spec:
  interval: 10m0s
  driftDetection:
    mode: enabled
  chart:
    spec:
      chart: rook-ceph
      version: v1.15.x
      sourceRef:
        kind: HelmRepository
        name: rook-ceph
        namespace: flux-system
