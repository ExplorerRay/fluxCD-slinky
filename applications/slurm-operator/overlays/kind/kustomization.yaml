apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: slinky
resources:
  - ../../base
generatorOptions:
  disableNameSuffixHash: true
configMapGenerator:
  - name: slurm-operator-values
    files:
      - values.yaml
