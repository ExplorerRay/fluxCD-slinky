---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: slurm
spec:
  interval: 10m0s
  driftDetection:
    mode: enabled
  valuesFrom:
    - kind: ConfigMap
      name: slurm-values
      valuesKey: values.yaml
  chart:
    spec:
      chart: slurm
      version: 1.2.x
      sourceRef:
        kind: HelmRepository
        name: slinky
        namespace: flux-system
