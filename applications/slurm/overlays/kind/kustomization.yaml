apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: slurm
resources:
  - ../../base
generatorOptions:
  disableNameSuffixHash: true
configMapGenerator:
  - name: slurm-values
    files:
      - values.yaml
